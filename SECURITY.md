# 安全说明

这是开发者预览固件，不是经过安全审计的产品。

- 配网热点开放，本地 HTTP 管理页没有登录保护；同网段可达者可能修改设备配置。请在可信环境首配，日常使用网络隔离/防火墙限制访问。
- 管理页面、MQTT 和 OTA 当前不提供端到端 TLS 安全保证。不要端口映射到公网；仅换端口不会启用加密。
- OTA 校验保护长度与内容完整性，不验证发布者身份；能发布 OTA 主题或替换下载内容的主体具有高权限。
- Broker 使用独立设备账号与最小主题权限；只允许受信任管理员/HA 发布亮度与 OTA 命令。不要公开匿名可写 Broker。
- 不回显密码不等于加密存储。设备 Flash 备份和网络抓包可能含凭据，必须当作秘密处理。
- Wi-Fi/MQTT 日常更新不应覆盖，但首次刷机、擦除、坏参数、配置结构变化和未经验证降级可能导致配置丢失。
- 传感器保持原机能力，项目不提供重新校准或测量精度认证。

报告问题请使用仓库的私人漏洞报告入口（如已启用）。普通 Issue 只提供脱敏复现信息，不上传密码、Token、完整 Flash、原始抓包或真实设备标识。
