# 参与开发

欢迎提交传感器协议、配网体验、网络兼容、OTA 和文档改进。

1. 先读 README 的硬件、授权和验收边界。
2. 不提交 MiCO SDK、第三方二进制、固件转储或私人凭据；SDK 适配须单独标明来源与许可。
3. 改动前后运行 `python scripts/run_tests.py`。主机测试通过不等于设备测试通过；PR 中明确区分两者。
4. 协议、分区、内核前缀、OTA 和参数结构变更必须说明迁移与回滚影响，不可直接移除保护校验。
5. 传感器 UART 只发送协议二进制，不能加调试文本。串口发送须使用既有锁。
6. 新的固件行为应递增应用版本；只改文档/主机发布工具不需要伪造设备版本。
7. 中文提交标题和正文需说明变更、影响范围、验证及风险；小修除外。

发布前运行 `python scripts/audit_public.py`，再人工审查 `git diff --cached`。工具仅作辅助，不能保证发现所有秘密。默认白名单刻意不包含开发机历史日志和旧版内部文档。
